To install the 'apscheduler' module, run the command `pip install apscheduler` in your terminal. You can also install extras by providing them as a comma-separated list inside the brackets, like this: `pip install apscheduler[psycopg, sqlalchemy]`. Be aware of potential issues that might arise during installation, such as those mentioned in the user guide.